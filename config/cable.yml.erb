redis: &default
  adapter: redis
  url: <%= ENV['REDIS_URL'] %>

production:
  channel_prefix: AliasMadness_production
  adapter: redis
  url: <%= ENV['REDIS_URL'] %>

development: *default
test:
  adapter: async

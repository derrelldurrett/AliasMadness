<!--
 Needs a form attribute in the html_options to tell it which form
 it belongs to ?
 -->
<%= select_tag 'winner',
               game_or_team_options_for_select(bracket,@game),
               {prompt: 'Choose winner...', node: node, class: 'game_winner', id: 'game_'+node}
%>